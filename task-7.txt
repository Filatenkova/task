Что было сделано:
1. Создана таблица через «SQL запрос»:
CREATE TABLE dev_city_statistics (
	ID int PRIMARY KEY AUTO_INCREMENT,
	NAME VARCHAR(255) NOT NULL,
	REVENUE DECIMAL(14, 2) NOT NULL,
	EXPENSES DECIMAL(14, 2) NOT NULL,
	COUNTS_RESIDENTS int NOT NULL
);

2. Подключен composer. Пространство имён пакета прописано в секции autoload. В init.php подключен /vendor/autoload.php.
3. Создана ORM CityStatisticsTable.php для установления связей между таблицей dev_city_statistics и ООП.
4. Создан простой компонент local/components/developer/simplecomponent для получения, обработки и вывода данных из таблицы dev_city_statistics.
5. В классе компонента SimpleComponent созданы методы getRevenueRating(), getExpensesRating(), getResidentsRating() для вычисления рейтинга.
6. Итоговые данные записаны в массив $arResult["ITEMS"].
7. Настроено кеширование компонента, значение времени кеширования передается через параметры компонента.
8. В шаблоне local/components/developer/simplecomponent/templates/simple/template.php интегрирована верстка,
заголовки вынесены в языковые файлы, настроен вывод полученных данных.
9. Настроен вывод ошибок
10. Создан раздел city, где подключен компонент
11. Результат задачи - http://filatenkova.ru/city/

Условия задачи:
Имеются данные:
- Название города
- Доходы общие
- Расходы общие
- Количество жителей

Необходимо реализовать структуру данных для хранения (данные приходят из вне и интерфейса для их редактирования через админку не нужно делать).

Так же необходимо реализовать вывод таблицы которая содержит следующие столбцы:
- Название
- Доходы общие
- Расходы общие
- Количество жителей
- Место в рейтинге по количеству жителей
- Место в рейтинге по средним доходам населения
- Место по средним расходам населения

Таблицу отсортировать по количеству жителей по убыванию
